[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "capstone-regime-portfolio"
version = "0.1.0"
description = "MScFE 690 Capstone: reliability-gated regime-aware dynamic portfolio allocation"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
